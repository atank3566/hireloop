{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://flextudio.local/meta-contract/v1/service/Flextudio.schema.json",
  "title": "Flextudio ServiceBinding",
  "description": "Flextudio 내장 시스템 호출. const.js 의 SYSTEM_ID.FLEXTUDIO='Flextudio' 에 매핑. 워크스페이스 사용자/메시지 도메인 기능 (GetUserList / QueryMessage / SendMessage). 런타임 구현: public/system/flextudio/flextudio.js.",
  "type": "object",
  "required": ["SystemID", "ModuleID"],
  "additionalProperties": true,
  "allOf": [
    { "$ref": "_base.schema.json#/$defs/commonBase" },
    {
      "properties": {
        "SystemID": { "const": "Flextudio", "description": "Flextudio 내장 시스템 호출 시 'Flextudio' 고정." },
        "ModuleID": {
          "type": "string",
          "enum": ["GetUserList", "QueryMessage", "SendMessage"],
          "description": "Flextudio 모듈. GetUserList=사용자 목록 조회 / QueryMessage=메시지 조회 / SendMessage=메시지 발송."
        },
        "UsePKey":       { "type": "boolean" },
        "UseParamBlock": { "type": "boolean" },
        "UseResultBlock":{ "type": "boolean" },
        "UseInnerBlock": { "type": "boolean" }
      }
    }
  ]
}
